package com.example.datastructure.a_core;


public class A01_ArraysUtilClass {
	/**
	 * ------------------------------------------------------------------------------------------------------------
	 * sort(int A[])														//full sorting		[Quick Sort]
	 * sort(int A[], int fromIndex, int toIndex)							//partial sorting	[Quick Sort]
	 * 
	 * sort(T A[], Comparator c)											//full sorting		[Quick Sort]
	 * sort(T A[], int fromIndex, int toIndex, Comparator c)				//partial sorting	[Quick Sort]
	 * 
	 * ------------------------------------------------------------------------------------------------------------
	 * binarySearch(int A[], int KEY)										//search KEY in A[] (full search)
	 * binarySearch(int A[], int fromIndex, int toIndex, int KEY)			//search KEY in A[] (partial search)
	 * 
	 * binarySearch(T A[], T key, Comparator c)								//search KEY in A[] (full search)
	 * binarySearch(T A[], int fromIndex, int toIndex, T key, Comparator c)	//search KEY in A[] (full search)
	 * 
	 * ------------------------------------------------------------------------------------------------------------
	 * swap(Object A[], int x, int y)				//swaps A[x] with A[y]
	 * 
	 * ------------------------------------------------------------------------------------------------------------
	 * equals(int A1[], int A2[])					//checks if both arrays contain same elements
	 * 
	 * ------------------------------------------------------------------------------------------------------------
	 * fill(int A[], int value)						//fill the A[] with all elements as 'value'
	 * 
	 * ------------------------------------------------------------------------------------------------------------
	 * Arrays.stream(A).max().getAsInt();			//find max/min
	 * 
	 * 
	 */
	
}
